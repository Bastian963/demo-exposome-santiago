# ADR 0003: Santiago CUT and v3.0 legacy retirement

## Decision

Santiago aggregate studies use five-digit CUT values as `spatial_id`.  Display
names and current web slugs remain compatibility fields in a versioned crosswalk.

The flat Santiago paths, city config and script wrappers retire in v3.0 only
after scientific parity, publication and documentation gates pass.
