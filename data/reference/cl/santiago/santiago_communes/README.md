# Santiago communes spatial reference

`spatial_units.geojson` is the canonical study support for Santiago. Its `spatial_id` is the five-digit CUT commune code; `legacy_name` and `slug` preserve compatibility with existing outputs and web profiles.
